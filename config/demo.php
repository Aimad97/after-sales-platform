<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Local demonstration credentials
    |--------------------------------------------------------------------------
    |
    | DemoDataSeeder is intentionally restricted to local and testing
    | environments. Override this password locally when a shared demo needs a
    | different credential; it must never be used for a production account.
    |
    */
    'password' => env('DEMO_PASSWORD', 'ServiceDeskDemo!2026'),
];
